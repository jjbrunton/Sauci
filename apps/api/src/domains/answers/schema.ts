// Registration contract: export this module from db/all-schema.ts when the
// application's Drizzle schema becomes authoritative. Migration 0003 is the
// current source of truth for this domain.
export const answersSchemaVersion = 3 as const;
