import { featureColors } from '../../theme';

/** Profile color palette constants */
export const PROFILE_COLORS = {
    ACCENT: featureColors.profile.accent,
    ACCENT_GRADIENT: featureColors.profile.gradient as [string, string],
} as const;
