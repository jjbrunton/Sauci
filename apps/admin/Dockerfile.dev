FROM node:20-bookworm-slim

WORKDIR /app

# Install the complete npm workspace from its committed lockfile.
COPY . .
RUN npm ci

# Build shared package
RUN npm run build -w @sauci/shared

WORKDIR /app/apps/admin

EXPOSE 3001

CMD ["npm", "run", "dev"]
